[project]
name = "document-rectifier"
version = "0.1.0"
description = "Interactive document rectification with OpenCV"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.2.5",
    "opencv-python>=4.11.0.86",
    "pillow>=11.2.1",
]

[project.scripts]
document-rectifier = "document_rectifier:main"

[build-system]
requires = ["setuptools>=80.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["document_rectifier"]

[tool.setuptools.package-data]
document_rectifier = ["static/*.html", "static/*.css", "static/*.js"]

[tool.uv]
package = true